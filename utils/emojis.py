
# ============================================================
#  DILBAR < 3  —  Central Emoji Config
#  Edit any emoji here and it updates everywhere in the bot.
# ============================================================

from utils.emojis import e
class Emojis:
    # ── Success / Failure ───────────────────────────────────
    green_tick   = f"{e.green_tick}"
    red_cross    = f"{e.red_cross}"
    tick         = f"{e.tick}"
    cross        = f"{e.cross}"
    cross_anim   = f"{e.cross_anim}"

    # ── Yes / No ────────────────────────────────────────────
    yes          = f"{e.yes}"
    no           = f"{e.no}"

    # ── UI / Navigation ─────────────────────────────────────
    dot_white    = f"{e.dot_white}"
    arrow        = f"{e.arrow}"
    disabled     = f"{e.disabled}"
    bla          = f"{e.bla}"

    # ── Status ──────────────────────────────────────────────
    online       = f"{e.online}"
    offline      = f"{e.offline}"
    idle         = f"{e.idle}"
    dnd          = f"{e.dnd}"
    status_anim  = f"{e.status_anim}"

    # ── Category Icons (used in help menu) ──────────────────
    antinuke     = f"{e.antinuke}"
    general      = f"{e.general}"
    music        = f"{e.music}"
    raidmode     = f"{e.raidmode}"
    welcome      = f"{e.welcome}"
    premium      = f"{e.premium}"
    ticket       = f"{e.ticket}"
    server       = f"{e.server}"
    moderation   = f"{e.moderation}"
    games        = f"{e.games}"
    utility      = f"{e.utility}"
    icon_voice   = f"{e.icon_voice}"
    voice        = f"{e.voice}"
    nsfw         = f"{e.nsfw}"
    encryption   = f"{e.encryption}"
    paint        = f"{e.paint}"
    verified     = f"{e.verified}"
    role         = f"{e.role}"

    # ── Info / Server Stats ─────────────────────────────────
    discord_icon = f"{e.discord_icon}"
    users        = f"{e.users}"
    python       = f"{e.python}"
    ping         = f"{e.ping}"
    developer    = f"{e.developer}"
    king         = f"{e.king}"
    war          = f"{e.war}"
    invite       = f"{e.invite}"
    support_team = f"{e.support_team}"
    dev_icon     = f"{e.dev_icon}"

    # ── Discord Badges ───────────────────────────────────────
    booster          = f"{e.booster}"
    hypersquad_events= f"{e.hypersquad_events}"
    hypersquad       = f"{e.hypersquad}"
    bravery          = f"{e.bravery}"
    brilliance       = f"{e.brilliance}"
    early_support    = f"{e.early_support}"
    active_dev       = f"{e.active_dev}"
    certified_mod    = f"{e.certified_mod}"
    partnered_owner  = f"{e.partnered_owner}"
    staff_anim       = f"{e.staff_anim}"
    partners         = f"{e.partners}"

    # ── Bot Badge Roles ──────────────────────────────────────
    staff_badge  = f"{e.staff_badge}"
    owners       = f"{e.owners}"
    friends      = f"{e.friends}"
    vip_badge    = f"{e.vip_badge}"
    bug_hunter2  = f"{e.bug_hunter2}"
    crown        = f"{e.crown}"
    diamond      = f"{e.diamond}"
    vip_friends  = f"{e.vip_friends}"
    early_anim   = f"{e.early_anim}"
    vip          = f"{e.vip}"
    bug_anim     = f"{e.bug_anim}"
    crown_anim   = f"{e.crown_anim}"
    staff_anim2  = f"{e.staff_anim2}"
    partner_anim = f"{e.partner_anim}"
    sponsor      = f"{e.sponsor}"
    friends2     = f"{e.friends2}"
    supporter    = f"{e.supporter}"
    vip2         = f"{e.vip2}"
    bug_hunter   = f"{e.bug_hunter}"

    # ── Music Player ────────────────────────────────────────
    volume_down    = f"{e.volume_down}"
    speaker_low    = f"{e.speaker_low}"
    emote1         = f"{e.emote1}"
    emote2         = f"{e.emote2}"
    emote3         = f"{e.emote3}"
    emote4         = f"{e.emote4}"
    jk_stop        = f"{e.jk_stop}"
    icon_voice2    = f"{e.icon_voice2}"
    black_diamond  = f"{e.black_diamond}"
    playing        = f"{e.playing}"
    jk_users       = f"{e.jk_users}"
    stage_icon     = f"{e.stage_icon}"
    uptime         = f"{e.uptime}"

    # ── Other UI ────────────────────────────────────────────
    fb_like        = f"{e.fb_like}"
    dislike        = f"{e.dislike}"
    icon_home      = f"{e.icon_home}"
    kekw           = f"{e.kekw}"
    dev_anim       = f"{e.dev_anim}"


e = Emojis()
