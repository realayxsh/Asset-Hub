# Core Discord library — discord.py 2.x (NOT py-cord, they conflict)
discord.py @ git+https://github.com/Rapptz/discord.py
# Jishaku debug extension — install with --no-deps (jishaku pypi depends on older discord.py)
# Run install.sh instead of pip install -r requirements.txt directly
aiohttp>=3.8.0
Flask==2.2.2
requests==2.28.1
async-timeout==4.0.2
python-dotenv==1.0.0
psutil>=5.9.0
httpx>=0.23.0
discord-ext-menus==1.1
import_expression
braceexpand
tabulate
